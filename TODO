* Support pkgname without version
* checksum all bins
* Support to pass pkgname with fullpath
* 9base has some warnings. push patch
  - there is no way to patch sources
* support ed and sed-like patches
